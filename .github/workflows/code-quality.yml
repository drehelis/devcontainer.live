name: Code Quality

jobs:
  check:
    uses: drehelis/improved-chainsaw/.github/workflows/code-quality.yml@main
    with:
      build: true
      prettier: true
      lint: true
      type-check: true